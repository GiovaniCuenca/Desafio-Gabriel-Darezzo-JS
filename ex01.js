var n1 = Number(window.prompt('Digite um número: '));
var n2 = Number(window.prompt('Digite um outro número: '));

var soma = n1 + n2;

window.alert(`A soma entre os números ${n1} e ${n2} é: ${soma}`);



/* Brincando um pouco com o setTimeout
setTimeout(() => {
  alert(`A soma entre os números ${n1} e ${n2} é: ${soma}`);
}, 3000);
*/


